# 🚀 Next.js Fullstack Lab

Este repositório é um laboratório prático dedicado ao estudo aprofundado do **Next.js**, cobrindo desde os fundamentos do **App Router** até padrões avançados de desenvolvimento Fullstack.

## 📚 Conteúdo em Estudo

O cronograma de estudos abrange os seguintes tópicos críticos para o desenvolvimento moderno com Next.js:

### Roteamento e Estrutura
* Rotas Dinâmicas (Nested e Catch-all Routes `[...slug]`)
* Grupos de Rotas (`(auth)`)
* Dynamic Head (SEO)
* Middleware

### Componentes e Renderização
* React Server Components (RSC)
* Client Components
* Hooks: `useRouter`, `usePathname` e `use()` (React 19)
* Otimização de Imagens (`next/image`)

### Data Fetching e Backend
* **Server-side Fetching:** Requisições diretamente em Server Components e estratégias de cache/memoização.
* **Client-side Fetching:** Padrões de consumo de dados em componentes de cliente.
* **Server Actions:** Manipulação de dados em formulários e fora deles, reduzindo a necessidade de APIs REST tradicionais.
* **API Routes:** Criação de endpoints internos e quando optar por APIs vs Server Actions.
